// A list of all Characters in the game
// Note that, This list MUST coorespond to anything that needs to be instantiated from that hero
// For example, to instantiate a heroes model, we use the NAME of the enum
public enum HeroList
{
    zeus, defender, archer
}

public enum PetList
{
    soap
}

public enum BossList
{
    Faphim
}